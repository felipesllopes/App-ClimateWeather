import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    greetings: {
        fontSize: 18,
        color: 'white',
        textAlign: 'center',
        marginBottom: 15
    },
    header: {
        alignItems: 'center',
    },
    degrees: {
        fontSize: 30,
        color: 'white',
    },
    locality: {
        fontSize: 17,
        color: 'white',
    },
    buttomReload: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 30,
        backgroundColor: '#666',
        padding: 4,
        borderRadius: 5,
    },
    reload: {
        fontSize: 17,
        color: 'white',
    },
});

export default styles;