import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    addInfo: {
        marginTop: 30,
        width: 350,
        borderRadius: 10,
        height: 200,
        backgroundColor: '#CCC',
        alignSelf: 'center',
    },
    tittleInfo: {
        fontSize: 22,
        fontWeight: 'bold',
        textAlign: 'center',
        marginVertical: 16,
    },
    tabInfo: {
        marginHorizontal: 50,
        alignItems: 'center',
    },
    textInfo: {
        fontSize: 18,
        fontWeight: 'bold',
    },
    props: {
        fontWeight: 'normal',
    },
})

export default styles;